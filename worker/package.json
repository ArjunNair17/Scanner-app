{
  "name": "protein-scanner-worker",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "node --import tsx --test test/*.test.ts"
  },
  "devDependencies": {
    "wrangler": "^4.28.0"
  }
}
